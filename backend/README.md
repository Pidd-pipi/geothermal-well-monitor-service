# Backend

This directory is the `geothermal-well-monitor-service` Go module. The executable entry point is `main.go` in this directory; it serves the embedded `web` assets and the HTTP API.

```bash
go test ./...
go build ./...
go run .
```
